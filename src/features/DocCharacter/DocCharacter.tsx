import { StyleSheet, Text, View } from "react-native";

import { StandardWrapper } from "../../infrastructure/ui/StandardWrapper";

export const DocCharacter = () => {
  return (
    <StandardWrapper>
      <View>
        <Text>DocCharacter</Text>
      </View>
    </StandardWrapper>
  );
};

const styles = StyleSheet.create({});
